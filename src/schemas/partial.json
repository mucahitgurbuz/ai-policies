{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://ai-policies.dev/schemas/partial.json",
  "title": "AI Policies Partial v2",
  "description": "Schema for policy partial frontmatter in Markdown files (v2.0)",
  "type": "object",
  "required": ["id"],
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique identifier for this partial within its package",
      "pattern": "^[a-z0-9-]+$",
      "minLength": 1,
      "maxLength": 100
    },
    "description": {
      "type": "string",
      "description": "Human-readable description of what this partial does",
      "maxLength": 500
    },
    "owner": {
      "type": "string",
      "description": "Team or individual responsible for maintaining this partial",
      "minLength": 1,
      "maxLength": 100
    },
    "tags": {
      "type": "array",
      "description": "Optional tags for categorizing and filtering partials",
      "items": {
        "type": "string",
        "pattern": "^[a-z0-9-]+$"
      },
      "uniqueItems": true
    },
    "providers": {
      "type": "array",
      "description": "IDE providers this partial applies to (if not specified, applies to all)",
      "items": {
        "type": "string",
        "enum": ["cursor", "copilot"]
      },
      "uniqueItems": true
    }
  },
  "additionalProperties": false
}
